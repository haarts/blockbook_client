name: blockbook
description: An API client for a Blockbook server (https://github.com/trezor/blockbook/blob/master/docs/api.md).
version: 1.1.4
author: Harm Aarts <harm@aarts.email>
homepage: https://github.com/haarts/blockbook_client

environment:
  sdk: '>=2.4.0 <3.0.0'

dependencies:
  http: ^0.12.0+3
  web_socket_channel: ^1.1.0

dev_dependencies:
  mock_web_server:
    git: https://github.com/haarts/MockWebServer.git
  pedantic: ^1.9.0
  test: ^1.14.3
